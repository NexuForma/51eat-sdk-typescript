// This file was auto-generated by Fern from our API Definition.

import * as FiveOneEat from "../../src/api/index";
import { FiveOneEatClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("TicketingClient", () => {
    test("holdTickets (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { tickets: [{ ticket_type_id: "ticket_type_id", quantity: 1 }] };
        const rawResponseBody = {
            data: {
                session_id: "session_id",
                expires_at: "expires_at",
                time_remaining_seconds: "time_remaining_seconds",
                holds: "holds",
            },
        };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/ticket-holds")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.ticketing.holdTickets({
            eventId: "eventId",
            tickets: [
                {
                    ticket_type_id: "ticket_type_id",
                    quantity: 1,
                },
            ],
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("holdTickets (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {
            tickets: [
                { ticket_type_id: "ticket_type_id", quantity: 10 },
                { ticket_type_id: "ticket_type_id", quantity: 10 },
            ],
        };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/ticket-holds")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.ticketing.holdTickets({
                eventId: "eventId",
                tickets: [
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                ],
            });
        }).rejects.toThrow(FiveOneEat.UnauthorizedError);
    });

    test("holdTickets (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {
            tickets: [
                { ticket_type_id: "ticket_type_id", quantity: 10 },
                { ticket_type_id: "ticket_type_id", quantity: 10 },
            ],
        };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/ticket-holds")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.ticketing.holdTickets({
                eventId: "eventId",
                tickets: [
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                ],
            });
        }).rejects.toThrow(FiveOneEat.NotFoundError);
    });

    test("holdTickets (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {
            tickets: [
                { ticket_type_id: "ticket_type_id", quantity: 10 },
                { ticket_type_id: "ticket_type_id", quantity: 10 },
            ],
        };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/ticket-holds")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(422)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.ticketing.holdTickets({
                eventId: "eventId",
                tickets: [
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                ],
            });
        }).rejects.toThrow(FiveOneEat.UnprocessableEntityError);
    });

    test("releaseTicketHold (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        server
            .mockEndpoint()
            .delete("/customer/events/eventId/ticket-holds/sessionId")
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.ticketing.releaseTicketHold({
            eventId: "eventId",
            sessionId: "sessionId",
        });
        expect(response).toEqual(undefined);
    });

    test("releaseTicketHold (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .delete("/customer/events/eventId/ticket-holds/sessionId")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.ticketing.releaseTicketHold({
                eventId: "eventId",
                sessionId: "sessionId",
            });
        }).rejects.toThrow(FiveOneEat.UnauthorizedError);
    });

    test("releaseTicketHold (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .delete("/customer/events/eventId/ticket-holds/sessionId")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.ticketing.releaseTicketHold({
                eventId: "eventId",
                sessionId: "sessionId",
            });
        }).rejects.toThrow(FiveOneEat.NotFoundError);
    });

    test("calculateTicketPrice (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { tickets: [{ ticket_type_id: "ticket_type_id", quantity: 1 }] };
        const rawResponseBody = {
            data: { subtotal: "subtotal", platform_fee: "platform_fee", total: "total", line_items: "line_items" },
        };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/calculate-price")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.ticketing.calculateTicketPrice({
            eventId: "eventId",
            tickets: [
                {
                    ticket_type_id: "ticket_type_id",
                    quantity: 1,
                },
            ],
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("calculateTicketPrice (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {
            tickets: [
                { ticket_type_id: "ticket_type_id", quantity: 10 },
                { ticket_type_id: "ticket_type_id", quantity: 10 },
            ],
        };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/calculate-price")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.ticketing.calculateTicketPrice({
                eventId: "eventId",
                tickets: [
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                ],
            });
        }).rejects.toThrow(FiveOneEat.UnauthorizedError);
    });

    test("calculateTicketPrice (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {
            tickets: [
                { ticket_type_id: "ticket_type_id", quantity: 10 },
                { ticket_type_id: "ticket_type_id", quantity: 10 },
            ],
        };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/calculate-price")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.ticketing.calculateTicketPrice({
                eventId: "eventId",
                tickets: [
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                ],
            });
        }).rejects.toThrow(FiveOneEat.NotFoundError);
    });

    test("calculateTicketPrice (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {
            tickets: [
                { ticket_type_id: "ticket_type_id", quantity: 10 },
                { ticket_type_id: "ticket_type_id", quantity: 10 },
            ],
        };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/calculate-price")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(422)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.ticketing.calculateTicketPrice({
                eventId: "eventId",
                tickets: [
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                ],
            });
        }).rejects.toThrow(FiveOneEat.UnprocessableEntityError);
    });

    test("createPaymentIntent (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { session_id: "session_id" };
        const rawResponseBody = {
            data: {
                payment_intent_id: "payment_intent_id",
                client_secret: "client_secret",
                session_id: "session_id",
                subtotal: "subtotal",
                platform_fee: "platform_fee",
                total_amount: "total_amount",
            },
        };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/payment-intent")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.ticketing.createPaymentIntent({
            eventId: "eventId",
            session_id: "session_id",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("createPaymentIntent (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { session_id: "session_id" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/payment-intent")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.ticketing.createPaymentIntent({
                eventId: "eventId",
                session_id: "session_id",
            });
        }).rejects.toThrow(FiveOneEat.UnauthorizedError);
    });

    test("createPaymentIntent (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { session_id: "session_id" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/payment-intent")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.ticketing.createPaymentIntent({
                eventId: "eventId",
                session_id: "session_id",
            });
        }).rejects.toThrow(FiveOneEat.NotFoundError);
    });

    test("createPaymentIntent (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { session_id: "session_id" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/payment-intent")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(422)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.ticketing.createPaymentIntent({
                eventId: "eventId",
                session_id: "session_id",
            });
        }).rejects.toThrow(FiveOneEat.UnprocessableEntityError);
    });

    test("confirmTicketOrder (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { payment_intent_id: "payment_intent_id", session_id: "session_id" };
        const rawResponseBody = { data: "data" };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/ticket-orders")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.ticketing.confirmTicketOrder({
            eventId: "eventId",
            payment_intent_id: "payment_intent_id",
            session_id: "session_id",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("confirmTicketOrder (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { payment_intent_id: "payment_intent_id", session_id: "session_id" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/ticket-orders")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.ticketing.confirmTicketOrder({
                eventId: "eventId",
                payment_intent_id: "payment_intent_id",
                session_id: "session_id",
            });
        }).rejects.toThrow(FiveOneEat.UnauthorizedError);
    });

    test("confirmTicketOrder (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { payment_intent_id: "payment_intent_id", session_id: "session_id" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/ticket-orders")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(422)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.ticketing.confirmTicketOrder({
                eventId: "eventId",
                payment_intent_id: "payment_intent_id",
                session_id: "session_id",
            });
        }).rejects.toThrow(FiveOneEat.UnprocessableEntityError);
    });
});
