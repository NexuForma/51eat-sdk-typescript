// This file was auto-generated by Fern from our API Definition.

import * as FiveOneEat from "../../src/api/index";
import { FiveOneEatClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("CustomerEventsClient", () => {
    test("releaseTicketHold (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        server
            .mockEndpoint()
            .delete("/customer/events/eventId/ticket-holds/cartId")
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.customerEvents.releaseTicketHold({
            eventId: "eventId",
            cartId: "cartId",
        });
        expect(response).toEqual(undefined);
    });

    test("releaseTicketHold (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .delete("/customer/events/eventId/ticket-holds/cartId")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.customerEvents.releaseTicketHold({
                eventId: "eventId",
                cartId: "cartId",
            });
        }).rejects.toThrow(FiveOneEat.UnauthorizedError);
    });
});
