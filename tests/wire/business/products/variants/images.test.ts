// This file was auto-generated by Fern from our API Definition.

import { FiveOneEatClient } from "../../../../../src/Client";
import { mockServerPool } from "../../../../mock-server/MockServerPool";

describe("ImagesClient", () => {
    test("upload", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        server.mockEndpoint().post("/business/products/variants/variant/images").respondWith().statusCode(200).build();

        const response = await client.business.products.variants.images.upload({
            variant: "variant",
        });
        expect(response).toEqual(undefined);
    });

    test("reorder", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        server
            .mockEndpoint()
            .put("/business/products/variants/variant/images/reorder")
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.business.products.variants.images.reorder({
            variant: "variant",
        });
        expect(response).toEqual(undefined);
    });

    test("delete", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        server
            .mockEndpoint()
            .delete("/business/products/variants/variant/images/image")
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.business.products.variants.images.delete({
            variant: "variant",
            image: "image",
        });
        expect(response).toEqual(undefined);
    });
});
