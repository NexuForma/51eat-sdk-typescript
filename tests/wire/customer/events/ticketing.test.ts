// This file was auto-generated by Fern from our API Definition.

import * as FiveOneEat from "../../../../src/api/index";
import { FiveOneEatClient } from "../../../../src/Client";
import { mockServerPool } from "../../../mock-server/MockServerPool";

describe("TicketingClient", () => {
    test("holdTickets (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { tickets: [{ ticket_type_id: "ticket_type_id", quantity: 1 }] };
        const rawResponseBody = {
            data: {
                ticket_cart_id: "ticket_cart_id",
                expires_at: "2024-01-15T09:30:00Z",
                time_remaining_seconds: "time_remaining_seconds",
                holds: "holds",
            },
        };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/ticket-holds")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.customer.events.ticketing.holdTickets({
            eventId: "eventId",
            tickets: [
                {
                    ticket_type_id: "ticket_type_id",
                    quantity: 1,
                },
            ],
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("holdTickets (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {
            tickets: [
                { ticket_type_id: "ticket_type_id", quantity: 10 },
                { ticket_type_id: "ticket_type_id", quantity: 10 },
            ],
        };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/ticket-holds")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.customer.events.ticketing.holdTickets({
                eventId: "eventId",
                tickets: [
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                ],
            });
        }).rejects.toThrow(FiveOneEat.UnauthorizedError);
    });

    test("holdTickets (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {
            tickets: [
                { ticket_type_id: "ticket_type_id", quantity: 10 },
                { ticket_type_id: "ticket_type_id", quantity: 10 },
            ],
        };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/ticket-holds")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.customer.events.ticketing.holdTickets({
                eventId: "eventId",
                tickets: [
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                ],
            });
        }).rejects.toThrow(FiveOneEat.NotFoundError);
    });

    test("holdTickets (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {
            tickets: [
                { ticket_type_id: "ticket_type_id", quantity: 10 },
                { ticket_type_id: "ticket_type_id", quantity: 10 },
            ],
        };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/ticket-holds")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(422)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.customer.events.ticketing.holdTickets({
                eventId: "eventId",
                tickets: [
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                ],
            });
        }).rejects.toThrow(FiveOneEat.UnprocessableEntityError);
    });

    test("calculatePrice (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { tickets: [{ ticket_type_id: "ticket_type_id", quantity: 1 }] };
        const rawResponseBody = {
            data: { subtotal: "subtotal", platform_fee: "platform_fee", total: "total", line_items: "line_items" },
        };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/calculate-price")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.customer.events.ticketing.calculatePrice({
            eventId: "eventId",
            tickets: [
                {
                    ticket_type_id: "ticket_type_id",
                    quantity: 1,
                },
            ],
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("calculatePrice (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {
            tickets: [
                { ticket_type_id: "ticket_type_id", quantity: 10 },
                { ticket_type_id: "ticket_type_id", quantity: 10 },
            ],
        };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/calculate-price")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.customer.events.ticketing.calculatePrice({
                eventId: "eventId",
                tickets: [
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                ],
            });
        }).rejects.toThrow(FiveOneEat.UnauthorizedError);
    });

    test("calculatePrice (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {
            tickets: [
                { ticket_type_id: "ticket_type_id", quantity: 10 },
                { ticket_type_id: "ticket_type_id", quantity: 10 },
            ],
        };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/calculate-price")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.customer.events.ticketing.calculatePrice({
                eventId: "eventId",
                tickets: [
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                ],
            });
        }).rejects.toThrow(FiveOneEat.NotFoundError);
    });

    test("calculatePrice (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {
            tickets: [
                { ticket_type_id: "ticket_type_id", quantity: 10 },
                { ticket_type_id: "ticket_type_id", quantity: 10 },
            ],
        };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/calculate-price")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(422)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.customer.events.ticketing.calculatePrice({
                eventId: "eventId",
                tickets: [
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                    {
                        ticket_type_id: "ticket_type_id",
                        quantity: 10,
                    },
                ],
            });
        }).rejects.toThrow(FiveOneEat.UnprocessableEntityError);
    });

    test("createPaymentIntent (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {
            billing_address: {
                line1: "line1",
                city: "city",
                state: "state",
                postal_code: "postal_code",
                country: "country",
            },
        };
        const rawResponseBody = {
            status: "status",
            client_secret: "client_secret",
            payment_intent_id: "payment_intent_id",
            subtotal: "subtotal",
            tax_amount: "tax_amount",
            tax_calculation: { id: "id", amount_total: "amount_total", tax_amount_exclusive: "tax_amount_exclusive" },
            shipping_amount: "shipping_amount",
            shipping_method: { key: "value" },
            platform_fee: "platform_fee",
            total_amount: "total_amount",
        };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/payment-intent")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.customer.events.ticketing.createPaymentIntent({
            eventId: "eventId",
            billing_address: {
                line1: "line1",
                city: "city",
                state: "state",
                postal_code: "postal_code",
                country: "country",
            },
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("createPaymentIntent (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {
            billing_address: {
                line1: "line1",
                city: "city",
                state: "state",
                postal_code: "postal_code",
                country: "country",
            },
        };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/payment-intent")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.customer.events.ticketing.createPaymentIntent({
                eventId: "eventId",
                billing_address: {
                    line1: "line1",
                    city: "city",
                    state: "state",
                    postal_code: "postal_code",
                    country: "country",
                },
            });
        }).rejects.toThrow(FiveOneEat.UnauthorizedError);
    });

    test("createPaymentIntent (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {
            billing_address: {
                line1: "line1",
                city: "city",
                state: "state",
                postal_code: "postal_code",
                country: "country",
            },
        };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/payment-intent")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(422)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.customer.events.ticketing.createPaymentIntent({
                eventId: "eventId",
                billing_address: {
                    line1: "line1",
                    city: "city",
                    state: "state",
                    postal_code: "postal_code",
                    country: "country",
                },
            });
        }).rejects.toThrow(FiveOneEat.UnprocessableEntityError);
    });

    test("confirmOrder (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { payment_intent_id: "payment_intent_id" };
        const rawResponseBody = { data: "data" };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/ticket-orders")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.customer.events.ticketing.confirmOrder({
            eventId: "eventId",
            payment_intent_id: "payment_intent_id",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("confirmOrder (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { payment_intent_id: "payment_intent_id" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/ticket-orders")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.customer.events.ticketing.confirmOrder({
                eventId: "eventId",
                payment_intent_id: "payment_intent_id",
            });
        }).rejects.toThrow(FiveOneEat.UnauthorizedError);
    });

    test("confirmOrder (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { payment_intent_id: "payment_intent_id" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/customer/events/eventId/ticket-orders")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(422)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.customer.events.ticketing.confirmOrder({
                eventId: "eventId",
                payment_intent_id: "payment_intent_id",
            });
        }).rejects.toThrow(FiveOneEat.UnprocessableEntityError);
    });

    test("releaseHold", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        server
            .mockEndpoint()
            .delete("/customer/events/eventId/ticket-holds/sessionId")
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.customer.events.ticketing.releaseHold({
            eventId: "eventId",
            sessionId: "sessionId",
        });
        expect(response).toEqual(undefined);
    });
});
