// This file was auto-generated by Fern from our API Definition.

import { FiveOneEatClient } from "../../../../src/Client";
import { mockServerPool } from "../../../mock-server/MockServerPool";

describe("TicketingClient", () => {
    test("holdTickets", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        server.mockEndpoint().post("/customer/events/event/ticket-holds").respondWith().statusCode(200).build();

        const response = await client.customer.events.ticketing.holdTickets({
            event: "event",
        });
        expect(response).toEqual(undefined);
    });

    test("releaseHold", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        server
            .mockEndpoint()
            .delete("/customer/events/event/ticket-holds/sessionId")
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.customer.events.ticketing.releaseHold({
            event: "event",
            sessionId: "sessionId",
        });
        expect(response).toEqual(undefined);
    });

    test("calculatePrice", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        server.mockEndpoint().post("/customer/events/event/calculate-price").respondWith().statusCode(200).build();

        const response = await client.customer.events.ticketing.calculatePrice({
            event: "event",
        });
        expect(response).toEqual(undefined);
    });

    test("createPaymentIntent", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        server.mockEndpoint().post("/customer/events/event/payment-intent").respondWith().statusCode(200).build();

        const response = await client.customer.events.ticketing.createPaymentIntent({
            event: "event",
        });
        expect(response).toEqual(undefined);
    });

    test("confirmOrder", async () => {
        const server = mockServerPool.createServer();
        const client = new FiveOneEatClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        server.mockEndpoint().post("/customer/events/event/ticket-orders").respondWith().statusCode(200).build();

        const response = await client.customer.events.ticketing.confirmOrder({
            event: "event",
        });
        expect(response).toEqual(undefined);
    });
});
