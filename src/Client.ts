// This file was auto-generated by Fern from our API Definition.

import * as FiveOneEat from "./api/index.js";
import { AuthenticationClient } from "./api/resources/authentication/client/Client.js";
import { BroadcastingClient } from "./api/resources/broadcasting/client/Client.js";
import { BusinessContentClient } from "./api/resources/businessContent/client/Client.js";
import { BusinessProfilesClient } from "./api/resources/businessProfiles/client/Client.js";
import { CustomerMessagingClient } from "./api/resources/customerMessaging/client/Client.js";
import { DiscoveryClient } from "./api/resources/discovery/client/Client.js";
import { EditorialsClient } from "./api/resources/editorials/client/Client.js";
import { EventsClient } from "./api/resources/events/client/Client.js";
import { MyAccountClient } from "./api/resources/myAccount/client/Client.js";
import { PushNotificationsClient } from "./api/resources/pushNotifications/client/Client.js";
import { RecordClickClient } from "./api/resources/recordClick/client/Client.js";
import { ServeAdClient } from "./api/resources/serveAd/client/Client.js";
import { TicketingClient } from "./api/resources/ticketing/client/Client.js";
import type { BaseClientOptions, BaseRequestOptions } from "./BaseClient.js";
import { type NormalizedClientOptionsWithAuth, normalizeClientOptionsWithAuth } from "./BaseClient.js";
import { mergeHeaders } from "./core/headers.js";
import * as core from "./core/index.js";
import { toJson } from "./core/json.js";
import * as environments from "./environments.js";
import { handleNonStatusCodeError } from "./errors/handleNonStatusCodeError.js";
import * as errors from "./errors/index.js";

export declare namespace FiveOneEatClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}
}

export class FiveOneEatClient {
    protected readonly _options: NormalizedClientOptionsWithAuth<FiveOneEatClient.Options>;
    protected _authentication: AuthenticationClient | undefined;
    protected _broadcasting: BroadcastingClient | undefined;
    protected _businessContent: BusinessContentClient | undefined;
    protected _businessProfiles: BusinessProfilesClient | undefined;
    protected _customerMessaging: CustomerMessagingClient | undefined;
    protected _discovery: DiscoveryClient | undefined;
    protected _editorials: EditorialsClient | undefined;
    protected _events: EventsClient | undefined;
    protected _myAccount: MyAccountClient | undefined;
    protected _pushNotifications: PushNotificationsClient | undefined;
    protected _ticketing: TicketingClient | undefined;
    protected _recordClick: RecordClickClient | undefined;
    protected _serveAd: ServeAdClient | undefined;

    constructor(options: FiveOneEatClient.Options = {}) {
        this._options = normalizeClientOptionsWithAuth(options);
    }

    public get authentication(): AuthenticationClient {
        return (this._authentication ??= new AuthenticationClient(this._options));
    }

    public get broadcasting(): BroadcastingClient {
        return (this._broadcasting ??= new BroadcastingClient(this._options));
    }

    public get businessContent(): BusinessContentClient {
        return (this._businessContent ??= new BusinessContentClient(this._options));
    }

    public get businessProfiles(): BusinessProfilesClient {
        return (this._businessProfiles ??= new BusinessProfilesClient(this._options));
    }

    public get customerMessaging(): CustomerMessagingClient {
        return (this._customerMessaging ??= new CustomerMessagingClient(this._options));
    }

    public get discovery(): DiscoveryClient {
        return (this._discovery ??= new DiscoveryClient(this._options));
    }

    public get editorials(): EditorialsClient {
        return (this._editorials ??= new EditorialsClient(this._options));
    }

    public get events(): EventsClient {
        return (this._events ??= new EventsClient(this._options));
    }

    public get myAccount(): MyAccountClient {
        return (this._myAccount ??= new MyAccountClient(this._options));
    }

    public get pushNotifications(): PushNotificationsClient {
        return (this._pushNotifications ??= new PushNotificationsClient(this._options));
    }

    public get ticketing(): TicketingClient {
        return (this._ticketing ??= new TicketingClient(this._options));
    }

    public get recordClick(): RecordClickClient {
        return (this._recordClick ??= new RecordClickClient(this._options));
    }

    public get serveAd(): ServeAdClient {
        return (this._serveAd ??= new ServeAdClient(this._options));
    }

    /**
     * Returns grouped results for each type. Use per_type to control how many results
     * appear per section. Optionally filter by category, city, or geo radius.
     *
     * @param {FiveOneEat.SearchRequest} request
     * @param {FiveOneEatClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link FiveOneEat.UnauthorizedError}
     * @throws {@link FiveOneEat.UnprocessableEntityError}
     *
     * @example
     *     await client.search({
     *         q: "wings",
     *         category: "restaurant",
     *         city: "Buffalo",
     *         cuisines: "italian,mexican",
     *         certifications: "kosher,halal"
     *     })
     */
    public search(
        request: FiveOneEat.SearchRequest,
        requestOptions?: FiveOneEatClient.RequestOptions,
    ): core.HttpResponsePromise<FiveOneEat.SearchResponse> {
        return core.HttpResponsePromise.fromPromise(this.__search(request, requestOptions));
    }

    private async __search(
        request: FiveOneEat.SearchRequest,
        requestOptions?: FiveOneEatClient.RequestOptions,
    ): Promise<core.WithRawResponse<FiveOneEat.SearchResponse>> {
        const {
            q,
            category,
            city,
            cuisines,
            certifications,
            radius_km: radiusKm,
            per_type: perType,
            lat,
            lng,
        } = request;
        const _queryParams: Record<string, unknown> = {
            q,
            category,
            city,
            cuisines,
            certifications,
            radius_km: radiusKm,
            per_type: perType,
            lat: lat != null ? toJson(lat) : undefined,
            lng: lng != null ? toJson(lng) : undefined,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.FiveOneEatEnvironment.Production,
                "customer/search",
            ),
            method: "GET",
            headers: _headers,
            queryParameters: { ..._queryParams, ...requestOptions?.queryParams },
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as FiveOneEat.SearchResponse, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 401:
                    throw new FiveOneEat.UnauthorizedError(_response.error.body as unknown, _response.rawResponse);
                case 422:
                    throw new FiveOneEat.UnprocessableEntityError(
                        _response.error.body as unknown,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.FiveOneEatError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "GET", "/customer/search");
    }

    /**
     * Make a passthrough request using the SDK's configured auth, retry, logging, etc.
     * This is useful for making requests to endpoints not yet supported in the SDK.
     * The input can be a URL string, URL object, or Request object. Relative paths are resolved against the configured base URL.
     *
     * @param {Request | string | URL} input - The URL, path, or Request object.
     * @param {RequestInit} init - Standard fetch RequestInit options.
     * @param {core.PassthroughRequest.RequestOptions} requestOptions - Per-request overrides (timeout, retries, headers, abort signal).
     * @returns {Promise<Response>} A standard Response object.
     */
    public async fetch(
        input: Request | string | URL,
        init?: RequestInit,
        requestOptions?: core.PassthroughRequest.RequestOptions,
    ): Promise<Response> {
        return core.makePassthroughRequest(
            input,
            init,
            {
                baseUrl: this._options.baseUrl ?? this._options.environment,
                headers: this._options.headers,
                timeoutInSeconds: this._options.timeoutInSeconds,
                maxRetries: this._options.maxRetries,
                fetch: this._options.fetch,
                logging: this._options.logging,
                getAuthHeaders: async () => (await this._options.authProvider.getAuthRequest()).headers,
            },
            requestOptions,
        );
    }
}
